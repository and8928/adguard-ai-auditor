<div align="center">

# 🛡️ AdGuard AI Auditor

**Интеллектуальный аудитор DNS-логов AdGuard Home - находит пропущенные трекеры и рекламу и выявляет ложные блокировки с помощью LLM.**
*Бэкенд на FastAPI | Дашборд в стиле Glassmorphism | Google Gemini / Vertex AI*

[![Python 3.13+](https://img.shields.io/badge/Python-3.13%2B-blue?logo=python&logoColor=white)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com/)
[![Pydantic v2](https://img.shields.io/badge/Pydantic-v2-E92063?logo=pydantic&logoColor=white)](https://docs.pydantic.dev/)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

[English](readme.md) · **Русский**

</div>

---

## 🎯 Что это такое

**AdGuard AI Auditor** - это интеллектуальный инструмент на FastAPI, который подключается к вашему серверу AdGuard Home, собирает логи DNS-запросов и проводит их аудит с помощью больших языковых моделей (Google Gemini, Vertex AI; OpenAI / Qwen - в планах).

Он автоматизирует анализ историю сетевого трафика: находит пропущенные трекеры и рекламу, выявляет ложные срабатывания (когда блокировка ломает легитимные приложения) и предлагает рекомендации по настройке фильтров.

---

## ✨ Возможности

| Возможность | Описание |
|---|---|
| 🔌 **Интеграция с AdGuard Home** | Автоматическое получение истории запросов (с поддержкой пагинации). |
| 🧹 **Очистка данных** | Удаление дубликатов и разбиение запросов на *Allowed* и *Blocked*. |
| 🛑 **Рекомендации по блокировке** | Находит телеметрию, трекеры и рекламу, прошедшие сквозь фильтр. |
| 🟢 **Рекомендации по разблокировке** | Выявляет ложные срабатывания (CDN, API), где блокировка может сломать сервис. |
| ⚠️ **Требует проверки** | Выделяет неоднозначные домены для ручного тестирования. |
| 🤖 **Поддержка нескольких LLM** | Google Gemini и Google Cloud Vertex AI; в планах - OpenAI ChatGPT и Qwen/локальные модели. |
| 📦 **Структурированный вывод** | ИИ возвращает строгий JSON с полем `reason` и уровнем `confidence` для каждой рекомендации. |
| 🎨 **Интерактивный дашборд** | UI в стиле Glassmorphism, тёмная тема и локализация EN/RU. |
| 🚀 **Прогресс в реальном времени** | Прогресс аудита транслируется вживую через Server-Sent Events (SSE). |
| 📝 **Пользовательские правила** | CRUD-управление пользовательскими промпт-правилами и переопределениями для ИИ. |
| ⚡ **Быстрые действия** | Применение рекомендованных блокировок/разблокировок прямо в AdGuard Home. |
| 🗂️ **Менеджер правил фильтра** | Просмотр текущих пользовательских правил AdGuard с живым поиском и фильтром по типу; смена типа правила (блок ↔ разрешение) или удаление прямо из дашборда — без запуска анализа. |

---

## 🛠 Технологический стек

| Слой | Стек |
|---|---|
| **Бэкенд** | Python 3.13+, FastAPI, Pydantic v2 |
| **Управление проектом** | Poetry |
| **Интеграция с ИИ** | `google-genai` (в планах: `openai`, `httpx`, `qwen`) |
| **Фронтенд** | Чистый HTML5, CSS (Glassmorphism, кастомные анимации) и JavaScript через шаблоны Jinja2 - без тяжёлой сборки на Node.js |
| **Логирование** | Кастомный логгер с выводом в консоль |

---

## 🚀 Установка и запуск

Рекомендуемый способ установки и запуска проекта - через **Poetry**.

### 1. Клонирование репозитория
```bash
git clone https://github.com/and8928/adguard-ai-auditor.git
cd adguard-ai-auditor
```

### 2. Установка зависимостей через Poetry
Настройте Poetry, чтобы виртуальное окружение создавалось внутри папки проекта:
```bash
poetry config virtualenvs.in-project true
poetry install
```

### 3. Настройка окружения
Создайте файл `.env` в корне проекта. Шаблон можно скопировать из `.env.example`:
```ini
# Настройки AdGuard
ADGUARD_BASE_URL="http://192.168.1.1"
ADGUARD_PORT=3333
ADGUARD_USER="your_user"
ADGUARD_PASSWORD="your_password"
AGH_SESSION="your_session"
ADGUARD_STEP_REQ=100

# Настройки Google Gemini
# Список моделей в виде JSON-строки (парсится Pydantic). Перебираются по порядку.
GEMINI_MODELS_NAME='["gemini-3-pro-preview","gemini-3-flash-preview", "gemini-3.1-pro-preview"]'
GEMINI_API_KEY = "gemini_key"

# Настройки Vertex AI (опционально, оставьте пустым, если не используется)
VERTEX_AI_MODELS_NAME='[]'
VERTEX_AI_API_KEY = ""

# Настройки OpenAI
OPENAI_MODEL_NAME="gpt-5-mini"
OPENAI_API_KEY = "openai_key"

# Установите True для подробного отладочного логирования
DEBUG_MOD = False
```

> [!NOTE]
> Автоматическое получение сессии пока в разработке, поэтому сейчас необходимо вручную указать cookie `AGH_SESSION` из вашего браузера.

### 4. Запуск приложения
```bash
poetry run uvicorn src.adguard_auditor.main:app --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`
Документация API (Swagger): `http://localhost:8000/docs`

### 5. Тестирование
В проекте есть набор тестов, покрывающий основную логику, эндпоинты API и интеграции с LLM:
```bash
poetry run pytest tests/ -v
```

> [!TIP]
> Все внешние вызовы (AdGuard Home, API LLM) мокаются, поэтому тесты запускаются без активных инстансов и API-ключей.

---

## 📡 Эндпоинты API

Основные эндпоинты в пространстве имён `/api/v1` (полный список - в Swagger по адресу `/docs`):

- `GET /` - Отдаёт интерактивный веб-дашборд (корень `/` автоматически перенаправляет сюда).
- `GET /get-raw-request-log?limit=100` - Получить сырые логи запросов с сервера AdGuard Home.
- `GET /get-response-log?limit=100` - Получить очищенные и сгруппированные логи (Allowed / Blocked).
- `GET /audit/stream` - SSE-эндпоинт, транслирующий прогресс и результаты аудита в реальном времени. Поддерживает `action=full|fetch|analyze`.
- `GET /audit/cache`, `POST /audit/cache/clear` - Просмотр или очистка закэшированных полученных/очищенных данных.
- `POST /to_block`, `POST /to_unblock`, `POST /to_delete` - Применить решения о блокировке/разблокировке или удалить пользовательские правила прямо в фильтрах AdGuard Home.
- `GET /get_actual_filter` - Получить текущие оптимизированные правила пользовательского фильтра из AdGuard Home (питает интерактивный менеджер правил фильтра).
- `GET /prompt-rules`, `POST /prompt-rules`, `GET /prompt-rules/{id}`, `PATCH /prompt-rules/{id}`, `DELETE /prompt-rules/{id}` - CRUD-эндпоинты для управления пользовательскими промпт-правилами и указаниями для ИИ.

---

## 📂 Структура проекта

```text
src/
├── adguard_auditor/
│   ├── main.py                 # Точка входа приложения FastAPI
│   ├── core/
│   │   ├── config.py           # Загрузка конфигурации через Pydantic Settings
│   │   ├── endpoints.py        # Построение URL для AdGuard Home
│   │   ├── prompts.py          # Системные промпты и шаблоны для ИИ
│   │   └── logger.py           # Настройка логирования
│   ├── api/
│   │   └── v1/
│   │       └── endpoints/
│   │           ├── audit.py    # Основные эндпоинты аудита и SSE
│   │           └── prompt_rules.py # CRUD-эндпоинты для правил
│   ├── services/
│   │   ├── adguard_client.py   # Клиент HTTP API AdGuard Home
│   │   ├── analysis_service.py # Парсинг, очистка логов и генерация правил
│   │   ├── cache.py            # In-memory кэш полученных/очищенных данных
│   │   ├── controller.py       # Главный контроллер потока данных
│   │   └── prompt_rules_service.py # Хранение промпт-правил
│   ├── schemas/
│   │   ├── adguard_models.py   # Pydantic-схемы структур AdGuard
│   │   ├── audit.py            # Модели запросов/ответов API
│   │   ├── prompt_rules.py     # Схемы промпт-правил
│   │   └── storage.py          # Схемы моделей хранения
│   └── frontend/
│       ├── static/
│       │   ├── style.css       # CSS тёмной темы с Glassmorphism
│       │   ├── i18n.js         # Локализация EN/RU
│       │   └── app.js          # Логика SPA и слушатель SSE
│       └── templates/
│           └── index.html      # Шаблон дашборда
├── gemini/
│   └── init.py                 # Клиент и обёртка API Google Gemini
├── vertex_ai/
│   └── init.py                 # Клиент и обёртка API Google Cloud Vertex AI
└── openai/
    └── init.py                 # Клиент OpenAI (в планах)
```

---

## 📝 Дорожная карта (планы)

### 🛠 Бэкенд и инфраструктура
- [ ] **Переход на `httpx`**: Заменить `requests` на `httpx` для лучшей поддержки async и производительности.

### 🤖 ИИ и промпт-инжиниринг
- [x] **Пользовательские промпт-правила**: Дополнение системных промптов правилами пользователя (например, считать «системные виджеты Windows» рекламой, а не нужным контентом).
  - *Пример*: ИИ предлагает `{ "domain": "assets.msn.com", "reason": "Required for Windows widgets", "confidence": "MEDIUM" }`, но пользователь хочет это заблокировать.
- [ ] **Локальный ИИ**: Добавить поддержку запуска локальных LLM (например, через Ollama или LM Studio).
- [ ] **Интеграции API**: Интегрировать API Qwen и повысить стабильность интеграции с OpenAI.

### 📊 Анализ и фильтрация
- [ ] **Категоризация ресурсов**:
  - [ ] Ресурсы, которые работают плохо.
  - [ ] Ресурсы, которые не работают.
  - [ ] Ресурсы, которые работают, но не должны (ложные срабатывания).
  - [ ] Ресурсы, которые нужно заблокировать (пропущенные трекеры/реклама).
  - [ ] Реклама, обнаруженная на конкретных сервисах.
- [x] **Управление фильтрами**: Чтение текущих пользовательских фильтров (`get_actual_filter`) и управление ими из дашборда — поиск, фильтр по типу, смена типа правила или удаление (`to_delete`).

### 🔐 Аутентификация
- [ ] **Авто-авторизация**: Реализовать автоматическую авторизацию в AdGuard Home (`_get_new_session`).

### 🎨 Фронтенд
- [x] **Менеджер правил фильтра**: Всегда доступная карточка «Текущие правила» с живым поиском, фильтром по типу, сменой типа на месте и удалением правил.
- [ ] **Раздел настроек**: Перенести язык и логин/пароль AdGuard в отдельный раздел «Настройки».
- [ ] **Интерактивная вкладка «Тест»**: Отправлять домены «требует проверки» в блокировку/разблокировку/игнор напрямую.
- [ ] **Улучшения UI**: Сейчас основной фокус на бэкенде FastAPI; интерфейс активно дорабатывается.

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Подробнее - в файле [LICENSE](LICENSE).
